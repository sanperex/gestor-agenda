{
  "name": "gestor-agenda-api",
  "version": "1.0.0",
  "description": "API REST del Gestor de Agenda (Taller 3)",
  "main": "server.js",
  "private": true,
  "scripts": {
    "dev": "node --watch-path=src --watch-path=server.js --env-file-if-exists=.env server.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.5"
  }
}
